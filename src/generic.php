<?php

/**
 * Simply a shortcut for !is_null()
 *
 * @param $input
 * @return bool
 */
function not_null($input) {
    return !is_null($input);
}

/**
 * Simply a shortcut for !empty()
 *
 * @param $input
 * @return bool
 */
function not_empty($input) {
    return !empty($input);
}